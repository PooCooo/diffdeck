{
  "name": "@diffdeck/cli",
  "version": "1.0.4",
  "description": "",
  "bin": {
    "diffdeck": "dist/diffdeck.mjs"
  },
  "files": [
    "dist",
    "skills"
  ],
  "scripts": {
    "test": "vitest run",
    "build:cli": "esbuild src/index.ts --bundle --external:commander --outfile=dist/diffdeck.mjs --format=esm --platform=node --banner:js='#!/usr/bin/env node'",
    "sync:ui": "node scripts/sync-ui.js",
    "build": "pnpm build:cli && pnpm sync:ui",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "@diffdeck/core": "workspace:*",
    "@diffdeck/shared": "workspace:*",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "esbuild": "^0.28.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}